pip install pgcli

docker run -it \

  -e POSTGRES_USER="root" \

  -e POSTGRES_PASSWORD="root" \

  -e POSTGRES_DB="ny_taxi" \

  -v "workspaces/AyoZoomcamp2024/week_1_basics_n_setup/2_docker_sql/ny_taxi_postgres_data:/var/lib/postgresql/data" \

  -p 5432:5432 \

  postgres:13


  'https://s3.amazonaws.com/nyc-tlc/trip+data/yellow_tripdata_2021-01.parquet'

  "$(pwd)/2_docker_sql/ny_taxi_postgres_data:/var/lib/postgresql/data" \

# login
pip install pgcli
pgcli -h localhost -p 5432 -u root -d ny_taxi

docker run -it \

  -e PGADMIN_DEFAULT_EMAIL="admin@admin.com" \

  -e PGADMIN_DEFAULT_PASSWORD="root" \

  -p 8080:80 \

 # --name pgadmin \

 # --net pg \

  dpage/pgadmin4


## Network Name

docker network create pg-network

## Network for Postgres
# name pgadmin admin will be able to discorver postgres

docker network create pg-network

docker run -it \
  -e POSTGRES_USER="root" \
  -e POSTGRES_PASSWORD="root" \
  -e POSTGRES_DB="ny_taxi" \
  -v "$(pwd)/2_docker_sql/ny_taxi_postgres_data:/var/lib/postgresql/data" \
  -p 5432:5432 \
  --network=pg-network \
  --name pg-database \
  postgres:13

## Network for Pgadmin
# name is least important here

docker run -it \
  -e PGADMIN_DEFAULT_EMAIL="admin@admin.com" \
  -e PGADMIN_DEFAULT_PASSWORD="root" \
  -p 8080:80 \
  --network=pg-network \
  --name pgadmin-2 dpage/pgadmin4




## Convert notebook to script

jupyter nbconvert --to=script upload-data.ipynb

URL="https://github.com/DataTalksClub/nyc-tlc-data/releases/download/yellow/yellow_tripdata_2021-01.csv.gz"

python ingest_data.py \
  --user=root \
  --password=root \
  --host=pg-database \
  --port=5432 \
  --db=ny_taxi \
  --table_name=yellow_taxi_trips \
  --url=${URL}


## Run on Docker
# Build image

docker build -t taxi_ingest:v001 .

URL="https://github.com/DataTalksClub/nyc-tlc-data/releases/download/yellow/yellow_tripdata_2021-01.csv.gz"

docker run -it --network=2_docker_sql_default \
  taxi_ingest:v001 \
    --user=root \
    --password=root \
    --host=pgdatabase \
    --port=5432 \
    --db=ny_taxi \
    --table_name=yellow_taxi_trips \
    --url=${URL}

pgcli -h pgdatabase -p 5431 -u root -d ny_taxi

# Return browser link of current directory
python -m http.server

# Returns ip address of my computer
ifconfig

docker-compose up

# run in detach mode. It means we get our terminal back
docker-compose up -d

# shut it down
docker-compose down


# returns list of networks
docker network ls"